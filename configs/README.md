# Configs

`rsp_default.example.json`은 공개된 recipe evidence를 요약한 작은 설정 예시입니다.

현재 trainer가 이 JSON을 자동으로 읽는 실행 계약은 아니며, 실제 실행 시에는 각 entrypoint의 CLI 인자와 Kaggle notebook recipe를 기준으로 값과 데이터 경로를 확인해야 합니다.
