# Scripts

코드는 기능별로 분리했습니다.

| Folder | 역할 |
| --- | --- |
| `data/` | RSP dataset build/verification |
| `train/` | Nemotron LoRA adapter training |
| `eval/` | local evaluator and boxed-answer checks |
| `package/` | Kaggle/Vast payload, adapter structure gate |
